# Deep Learning
# RTX 5090 (sm_120) 需要 PyTorch 2.6+ with CUDA 12.8
# 安装命令: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.6.0
torchvision>=0.21.0
# SOTA Models
monai>=1.3.0              # Swin-UNetR + SegResNet
nnunetv2>=2.2             # nnUNet v2
dynamic-network-architectures>=0.3  # PlainConvUNet (nnUNet 核心架构)
timm>=0.9.0               # Vision Transformer backbones
einops>=0.7.0             # Tensor operations for transformers

# SegResNet 已包含在 MONAI 中，无需额外依赖

# Core
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
tqdm>=4.65.0
pyyaml>=6.0
omegaconf>=2.3.0

# Image I/O & augmentation
pillow>=9.5.0
opencv-python>=4.7.0
albumentations>=1.3.0
scikit-image>=0.21.0

# Medical formats (nii/nii.gz, mhd/mha, dicom)
SimpleITK>=2.2.0
nibabel>=5.0.0
pydicom>=2.3.0

# Logging/visualization
tensorboard>=2.12.0
matplotlib>=3.7.0

# Testing
pytest>=7.3.0
